const chapter04 = require('./chapter04')
const SinglyList = require('../util/SinglyList')

describe('Chapter 04', () => {
    describe('routeBetweenNodes', () => {
        it('returns correct results', () => {
            expect(chapter04.routeBetweenNodes()).toEqual()
        })
    })

})
